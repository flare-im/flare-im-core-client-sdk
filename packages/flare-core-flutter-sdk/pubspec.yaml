name: flare_core_flutter_sdk
description: Ready-to-use Flutter/Dart client SDK for Flare Core IM.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  test: ^1.25.8
