package flare_core_harmony_cangjie_sdk.model.event

// GENERATED. Do not edit by hand.
// Collection/map payloads are represented as raw JSON strings at the bridge boundary.
// Common event envelope emitted by every platform SDK event stream.
public class SdkEventEnvelope {
    // wire: `eventId`. Unique event id generated by the SDK adapter.
    public let eventId: String
    // wire: `kind`. Top-level event domain.
    public let kind: SdkEventKind
    // wire: `name`. Domain-specific event name.
    public let name: String
    // wire: `occurredAt`. Event time in milliseconds.
    public let occurredAt: UInt64
    // wire: `traceId`. Optional. Trace id for diagnostics when available.
    public let traceId: ?String
    // wire: `payload`. Raw event payload for forward-compatible consumers.
    public let payload: String

    public init(eventId!: String, kind!: SdkEventKind, name!: String, occurredAt!: UInt64, traceId!: ?String, payload!: String) {
        this.eventId = eventId
        this.kind = kind
        this.name = name
        this.occurredAt = occurredAt
        this.traceId = traceId
        this.payload = payload
    }
}
