// GENERATED. Do not edit by hand.
import { SdkEventKind } from './sdk_event_kind';

/** Common event envelope emitted by every platform SDK event stream. */
export interface SdkEventEnvelope {
  /** wire: `eventId`. Unique event id generated by the SDK adapter. */
  eventId: string;
  /** wire: `kind`. Top-level event domain. */
  kind: SdkEventKind;
  /** wire: `name`. Domain-specific event name. */
  name: string;
  /** wire: `occurredAt`. Event time in milliseconds. */
  occurredAt: number;
  /** wire: `traceId`. Trace id for diagnostics when available. */
  traceId?: string;
  /** wire: `payload`. Raw event payload for forward-compatible consumers. */
  payload: Record<string, Object>;
}
